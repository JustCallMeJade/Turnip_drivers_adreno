diff --git a/meson.build b/meson.build
index 9dffae1f7f9..3a1b2c3d4e5 100644
--- a/meson.build
+++ b/meson.build
@@ -461,7 +461,7 @@ if with_glx != 'disabled'
     if not with_gallium
       error('xlib based GLX requires at least one gallium driver')
-    elif not with_gallium_softpipe
-      error('xlib based GLX requires softpipe or llvmpipe.')
+    elif not with_gallium_softpipe and not with_gallium_llvmpipe and not with_gallium_virgl
+      error('xlib based GLX requires softpipe, llvmpipe, or virgl.')
     elif with_dri
       error('xlib conflicts with any dri driver')
     endif
diff --git a/src/gallium/auxiliary/hud/hud_context.c b/src/gallium/auxiliary/hud/hud_context.c
index f5b11c42509..2ef6a291942 100644
--- a/src/gallium/auxiliary/hud/hud_context.c
+++ b/src/gallium/auxiliary/hud/hud_context.c
@@ -1757,7 +1757,8 @@ hud_set_draw_context(struct hud_context *hud, struct cso_context *cso,
    {
       static const char *vertex_shader_text = {
          "VERT\n"
-         "DCL IN[0..1]\n"
+         "DCL IN[0]\n"
+         "DCL IN[1]\n"
          "DCL OUT[0], POSITION\n"
          "DCL OUT[1], COLOR[0]\n" /* color */
          "DCL OUT[2], GENERIC[0]\n" /* texcoord */
@@ -1801,7 +1802,8 @@ hud_set_draw_context(struct hud_context *hud, struct cso_context *cso,
        * to match the varyings in fs_text */
       static const char *vertex_shader_text = {
          "VERT\n"
-         "DCL IN[0..1]\n"
+         "DCL IN[0]\n"
+         "DCL IN[1]\n"
          "DCL OUT[0], POSITION\n"
          "DCL OUT[1], GENERIC[0]\n" /* texcoord */
          /* [0] = color,
diff --git a/src/gallium/auxiliary/target-helpers/inline_sw_helper.h b/src/gallium/auxiliary/target-helpers/inline_sw_helper.h
index 494dece4d27..7c8d9e0f1a2 100644
--- a/src/gallium/auxiliary/target-helpers/inline_sw_helper.h
+++ b/src/gallium/auxiliary/target-helpers/inline_sw_helper.h
@@ -39,17 +39,17 @@ sw_screen_create_named(struct sw_winsys *winsys, const char *driver)
       screen = llvmpipe_create_screen(winsys);
 #endif
 
-#if defined(GALLIUM_VIRGL)
+#ifdef GALLIUM_VIRGL
    if (screen == NULL && strcmp(driver, "virpipe") == 0) {
       struct virgl_winsys *vws;
       vws = virgl_vtest_winsys_wrap(winsys);
       screen = virgl_create_screen(vws, NULL);
    }
 #endif
 
 #if defined(GALLIUM_SOFTPIPE)
    if (screen == NULL && strcmp(driver, "softpipe") == 0)
       screen = softpipe_create_screen(winsys);
 #endif
 
 #if defined(GALLIUM_ZINK)
@@ -71,6 +71,9 @@ sw_screen_create_vk(struct sw_winsys *winsys, bool sw_vk)
    const char *drivers[] = {
       (sw_vk ? "" : debug_get_option("GALLIUM_DRIVER", "")),
 #if defined(GALLIUM_D3D12)
       (sw_vk || only_sw) ? "" : "d3d12",
 #endif
+#if defined(GALLIUM_VIRGL)
+      (sw_vk ? "" : "virpipe"),
+#endif
 #if defined(GALLIUM_LLVMPIPE)
       "llvmpipe",
diff --git a/src/gallium/auxiliary/vl/vl_winsys_xlib_swrast.c b/src/gallium/auxiliary/vl/vl_winsys_xlib_swrast.c
index 1d3b4a8e3c2..a1b2c3d4e5f 100644
--- a/src/gallium/auxiliary/vl/vl_winsys_xlib_swrast.c
+++ b/src/gallium/auxiliary/vl/vl_winsys_xlib_swrast.c
@@ -150,7 +150,8 @@ vl_swrast_texture_from_drawable(struct vl_screen *vscreen, void *drawable)
 
    XWindowAttributes x11_window_attrs = {};
    XGetWindowAttributes(scrn->display, x11_window, &x11_window_attrs);
-   enum pipe_format x11_window_format = vl_dri2_format_for_depth(&scrn->base, x11_window_attrs.depth);
+   enum pipe_format x11_window_format = x11_window_attrs.depth == 24 ?
+      PIPE_FORMAT_B8G8R8X8_UNORM : PIPE_FORMAT_B8G8R8A8_UNORM;
 
    bool needs_new_back_buffer_allocation = true;
    if (scrn->drawable_texture) {
diff --git a/src/gallium/drivers/virgl/virgl_screen.c b/src/gallium/drivers/virgl/virgl_screen.c
index cc2b7eb054b..0e40ec63914 100644
--- a/src/gallium/drivers/virgl/virgl_screen.c
+++ b/src/gallium/drivers/virgl/virgl_screen.c
@@ -905,13 +905,13 @@ static void virgl_flush_frontbuffer(struct pipe_screen *screen,
                                     struct winsys_handle *whandle)
 {
    struct virgl_screen *vscreen = virgl_screen(screen);
    struct virgl_winsys *vws = vscreen->vws;
    struct virgl_resource *vres = virgl_resource(res);
-   struct virgl_context *vctx = virgl_context(ctx);
+   struct virgl_context *vctx = ctx ? virgl_context(ctx) : NULL;
 
-   if (vws->flush_frontbuffer) {
-      virgl_flush_eq(vctx, vctx, NULL);
+   if (vws->flush_frontbuffer && vctx) {
+      virgl_flush_eq(vctx, vctx, NULL);
       vws->flush_frontbuffer(vws, vres->hw_res, level, layer, winsys_drawable_handle,
                              sub_box);
    }
 }
diff --git a/src/mesa/main/context.c b/src/mesa/main/context.c
index 2b810b0d863..2b810b0d863 100644
--- a/src/mesa/main/context.c
+++ b/src/mesa/main/context.c
@@ -1485,14 +1485,14 @@ _mesa_make_current( struct gl_context *newCtx,
       if (!check_compatible(newCtx, drawBuffer)) {
          _mesa_warning(newCtx,
               "MakeCurrent: incompatible visuals for context and drawbuffer");
-         return GL_FALSE;
+         return GL_FALSE;
       }
    }
    if (newCtx && readBuffer && newCtx->WinSysReadBuffer != readBuffer) {
       if (!check_compatible(newCtx, readBuffer)) {
          _mesa_warning(newCtx,
               "MakeCurrent: incompatible visuals for context and readbuffer");
-         return GL_FALSE;
+         return GL_FALSE;
       }
    }
